1. Добавила возможность добавить комментарий 

2. Нашла Баг двойной отрисовки при первой загрузке (помните у нас два раза массив формировался на 100  и 101 объект и это 
еще отражалась в виде дополнительного пустого блока сверху у Постов)??? 
Смотрите файл postStore.js
Это потому что отрабатывались обе функции и getPosts(posts) и addPost(post) - в которой в post мы ничего не передавали от того и пустой блок вылезал
Добавила условие проверки  если post не пустой - тогда выполняем

 addPost(post) {
    if (post.lenght) {
      this.posts = [post, ...this.posts];
      this.change();
    }
  } 